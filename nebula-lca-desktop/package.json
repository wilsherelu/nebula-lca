{
  "name": "nebula-lca-desktop",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:web": "node scripts/build-web.js",
    "build:api": "node scripts/build-api.js",
    "desktop:build": "npm run build:web && npm run build:api && npm run build",
    "desktop:dist": "npm run desktop:build && electron-builder --config electron-builder.yml",
    "desktop:smoke": "npm run build && electron dist/main.js --smoke"
  },
  "dependencies": {
    "electron-is-dev": "^3.0.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "electron": "^33.2.1",
    "electron-builder": "^25.1.8",
    "typescript": "^5.8.3"
  }
}
